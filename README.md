# Que-handler-for-cpu-without-OS-lock
the c++ code is meant to handle threads at the atomic operations rather than 0S-level locks




Compile and Run: Use a terminal with a C++ compiler installed:

Bash
g++ -std=c++20 -O3 src/main.cpp -o benchmark -pthread
./benchmark
